package
{
	import starling.display.Button;
	import starling.textures.Texture;
	
	public class UnstyledButton extends Button
	{
		public function UnstyledButton(upState:Texture, text:String="", downState:Texture=null, overState:Texture=null, disabledState:Texture=null)
		{
			super(upState, text, downState, overState, disabledState);
			
			
			
		}
	}
}